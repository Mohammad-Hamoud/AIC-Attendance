# Netlify build config for this Vite single-page app.
[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: every route serves index.html (client-side routing).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
